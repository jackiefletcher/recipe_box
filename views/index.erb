<h1>Recipe Box</h1>

<p><a href="/recipes">Add a new recipe</a></p>
<p><a href="/ingredients">Add a new ingredient</a></p>

<h3>Here are all the ingredients in the database:</h3>

<% if @ingredients.any?() %>
  <p>Click on an ingredient to see information about them and mark which recipes they belong to:</p>

  <ul>
    <% @ingredients.each do |ingredient| %>
      <li><a href="/ingredients/<%= ingredient.id() %>"><%= ingredient.food() %></a></li>
    <% end %>
  </ul>
<% else %>
  <p>There are no ingredients added yet</p>
<% end %>

<h3>Here are all the recipes in the database:</h3>

<% if @recipes.any?() %>
  <p>Click on a recipe to see information about it and add ingredients:</p>

  <ul>
    <% @recipes.each do |recipe| %>
      <li><a href="/recipes/<%= recipe.id() %>"><%= recipe.name() %></a></li>
      <% end %>
  </ul>
  <% else %>
    <p>There are no recipes</p>
  <% end %>
